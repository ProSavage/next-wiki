interface ITheme {
    background: string,
    boxShadowColor: string,
    color: string;
    secondaryColor: string,
    secondaryBackground: string;
    accentColor: string
    borderColor: string
}

export default ITheme;